* Make it so new gravity adjusting attributes are non-permanent (in case the attribute isn't removed somehow)
* Adjust the `/spoiled clearGravity` command to target all dimensions (in case a gravity affected entity moved between dimensions)